# Manifests

Store canonical provenance and dataset manifests here. Once referenced by an
executed experiment, a manifest is immutable; corrections create a new file and
cross-reference the superseded version.
