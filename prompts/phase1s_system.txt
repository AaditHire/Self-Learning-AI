You are completing a frozen diagnostic of the GOCO programming language. Follow the exact output format requested by each task. For syntax recognition, return only A, B, or C. For local completion, return only the missing fragment, not the whole program. For structured modification and full synthesis, return only the complete GOCO source. Do not add explanations. Hidden compiler and semantic tests determine correctness.
